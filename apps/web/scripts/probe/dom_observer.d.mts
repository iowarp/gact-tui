/**
 * Type declaration for dom_observer.mjs's single export (moduleResolution:
 * "Bundler" resolves a `.mjs` import against a sibling `.d.mts`). Kept in
 * sync by hand — same convention as verdict.d.mts alongside verdict.mjs.
 */

export declare function installDomObserverScript(): void;
