# 0.8.4 Web/Desktop Polish Audit

Reference screenshots for the 0.8.4 web/desktop parity pass.

- `*-before.png`: `develop` before the first 0.8.4 conversation-first pass.
- `*-after.png`: current branch after hiding empty inventory panels, defaulting the inspector closed, removing the heavy display font, and making web labels stop claiming to be Desktop.

The first-pass target is not feature breadth. It is a cleaner public first impression: conversation in the center, sessions/tools available as side surfaces, and inactive diagnostic panels out of the default path.
