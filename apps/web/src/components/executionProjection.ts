/**
 * Barrel for the execution-projection module: re-exports the projection
 * model, helpers, timeline, and types used to render the agent run tree.
 */
export { projectedTranscriptMessages } from './executionProjectionModel.js';
