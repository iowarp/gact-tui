/**
 * stripClioScaffolding moved to the one spec'd home for the web client's
 * transitional prose-heuristic presentation filters — see
 * ./presentationFilters.ts and contract/SPEC.md Appendix ("Transitional client
 * presentation filters (non-normative)"). Re-exported here so any lingering
 * importer keeps working; new call sites import from presentationFilters.ts.
 */
export { stripClioScaffolding } from './presentationFilters.js';
