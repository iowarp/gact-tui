target/
Cargo.lock
gen/schemas/
# Embedded backend runtime for the bundled installer variant.
# Built on demand by the embedding brand's build script (e.g.
# iowarp/clio-agent install/build-gact-runtime.{sh,ps1}); never committed.
gact-runtime/
