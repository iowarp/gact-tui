{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities allowed to the default window. Locked down per the security posture in docs/archive/apps-design/03-architecture.md — only the OS bridges we actually use are exposed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "core:window:allow-set-size",
    "core:window:allow-minimize",
    "core:window:allow-maximize",
    "notification:default",
    "dialog:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
